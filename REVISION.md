# Version 0.2.0
- Bug fix: Fixed a but where `--edit` flag was not working. Io requires an allocator to be passed when initializing `std.Io.Threaded`
- [[ready_tail_tzs]] Added `--owner` flag to the `todo` command to set the owner of a task
- [[ready_tail_tzs]] Added a `GILA_USER` environment variable to configure default username
